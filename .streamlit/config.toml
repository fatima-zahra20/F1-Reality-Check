[theme]
base = "light"
primaryColor = "#E10600"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#31333F"
font = "sans serif"

[server]
# The bundle is ~23 MB uncompressed and cached in memory per session.
maxUploadSize = 50
